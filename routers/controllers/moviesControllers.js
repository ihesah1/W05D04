const getAllMovies = (err, data) => {
    nweM = JSON.parse(data.toString());
    // parse to javascript and the string to read it ( buffer )
  }''
const getMovieById =
const getFav =
const createNewMovie =
const updateMovieDetails =
const deleteMovie =